function findThieleSmall(i)

printf('Attempting to find %f', names(0));

    switch i
        case 1
            Fs = 1 / (2*pi*sqrt(Mms * Cms));
        case 2
            Qms = sqrt(Mms / Cms) / Rms;
        case 3
            Qes = sqrt(Mas/Cas) / Rae;
        case 4
            Rae = (Bl^2) / (Re * Sd^2);
            Ras = Rms / Sd^2;
            Qts = Qms * Qes / (Qms + Qes);
        case 5
            Cms
        case 6
            ts('Mms');
        case 7
            Rms = sqrt(Mms/Cms) / Qms;
        case 8
            ts('Le');
        case 9
            ts('Re');
        case 10
            ts('Vd');
        case 11
            Vas = po * c^2 * Sd^2 * Cms;
        case 12
            ts('Bl');
        case 13
            ts('Sd');
    end
end